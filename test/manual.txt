Dựa trên code hiện tại, tôi sẽ giải thích tại sao ESP32 bị crash và jamming không hiệu quả:

## **Phân tích nguyên nhân crash:**

### **1. Watchdog Timer Reset:**
```cpp
// Vấn đề trong loop chính:
while (!stopJamming && isJamming) {
    // Loop này chạy cực nhanh không có yield()
    delayMicroseconds(50); // Quá ngắn, watchdog không được feed
}
```
- ⚠️ **ESP32 watchdog timer** cần được feed mỗi ~3-8 giây
- ⚠️ **delayMicroseconds(50)** quá ngắn, không đủ cho system tasks
- ⚠️ **Không có yield()** cho phép ESP32 xử lý background tasks

### **2. WiFi Hardware Overload:**
```cpp
// Gửi packets quá nhanh:
for (int i = 0; i < 100; i++) {
    esp_wifi_80211_tx(...);
    delayMicroseconds(10); // Quá nhanh!
}

for (int i = 0; i < 200; i++) {
    esp_wifi_80211_tx(...);
    delayMicroseconds(5); // Cực kỳ nhanh!
}
```
- ⚠️ **TX buffer overflow** - ESP32 không kịp xử lý
- ⚠️ **Hardware stress** do gửi 300+ packets liên tục
- ⚠️ **Memory fragmentation** do random MAC generation quá nhiều

### **3. Channel Switching Quá Thường Xuyên:**
```cpp
// Multi-channel chaos mỗi 20 waves:
if (waveCount % 20 == 0) {
    for (int ch = 1; ch <= 13; ch++) {
        esp_wifi_set_channel(ch, WIFI_SECOND_CHAN_NONE);
        delay(5); // Quá ngắn để ổn định
    }
}
```
- ⚠️ **Channel switching overhead** rất cao
- ⚠️ **WiFi stack confusion** do thay đổi quá nhanh
- ⚠️ **Hardware không kịp sync** với frequency changes

## **Tại sao jamming không hiệu quả:**

### **1. ESP32 Hardware Limitations:**
- 📡 **ESP32 không thể gửi deauth frames** (0xC0) - bị firmware block
- 📡 **Chỉ có thể beacon flooding** - impact hạn chế
- 📡 **Single antenna** - không thể jam nhiều channels đồng thời

### **2. Modern WiFi Resistance:**
```cpp
// Beacon flooding chỉ tạo noise:
beaconPacket[37] = targetSSID.length();
memcpy(&beaconPacket[38], targetSSID.c_str(), targetSSID.length());
// → Devices vẫn có thể phân biệt real vs fake AP
```
- 🛡️ **Modern routers có CRC checking** - filter fake frames
- 🛡️ **Devices stick to known BSSIDs** - không bị confuse bởi fake APs
- 🛡️ **802.11w protection** trên newer networks

### **3. Network Stack Resilience:**
- 🔄 **Auto-reconnect mechanisms** khôi phục kết nối nhanh
- 🔄 **Channel hopping resistance** - devices follow AP channel changes
- 🔄 **Signal strength priority** - real AP thường mạnh hơn fake

## **Tại sao crash xảy ra theo pattern:**

```
🌪️ Multi-channel chaos mode    ← Bắt đầu overload
🎭 Fake AP injection          ← Tăng memory pressure  
🌪️ Multi-channel chaos mode    ← Hardware stress peak
---- ESP32 CRASH ----         ← Watchdog timeout
ets Jul 29 2019 12:21:46      ← Boot message
```

### **Timeline crash:**
1. **0-10s**: Normal jamming, ESP32 cope được
2. **10-20s**: Multi-channel chaos tăng load
3. **20-30s**: Memory fragmentation tích tụ
4. **30s+**: Watchdog timeout → **CRASH**

## **Hiệu quả thực tế của jamming:**

### **Những gì code hiện tại CÓ THỂ làm:**
- ✅ **Tạo noise** trên WiFi spectrum
- ✅ **Làm chậm tốc độ** internet một chút
- ✅ **Gây khó khăn** cho WiFi scanner
- ✅ **Tăng latency** của mạng target

### **Những gì code KHÔNG THỂ làm:**
- ❌ **Ngắt kết nối** clients (cần real deauth)
- ❌ **Block WiFi hoàn toàn** (chỉ degradation)
- ❌ **Penetrate modern security** (WPA3, 802.11w)
- ❌ **Jam multiple bands** (chỉ 2.4GHz)

## **Kết luận:**

**Code hiện tại bị crash vì:**
- Overload hardware với quá nhiều packets
- Không feed watchdog timer đúng cách
- Channel switching quá aggressive

**Jamming không hiệu quả vì:**
- ESP32 hardware limitations (no real deauth)
- Modern WiFi stack resistance 
- Chỉ có thể tạo noise, không thể force disconnect

**→ Cần optimize code để stable, nhưng không thể làm jamming mạnh hơn do hardware limit.**